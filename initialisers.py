from datetime import datetime


class Player:
    __LVL = 1
    __HEALTH = 100

    # В __slots__ нужно указывать искаженные имена для приватных переменных
    __slots__ = ["_Player__lvl", "_Player__health", "_Player__born"]

    def __init__(self) -> None:
        self.__lvl = Player.__LVL
        self.__health = Player.__HEALTH
        self.__born = datetime.now()

    @property
    def lvl(self):

        time_diff = datetime.now() - self.__born
        return self.__lvl, str(time_diff)

    @lvl.setter
    def lvl(self, numeric: int):
        self.__lvl += Player.__type_test(numeric)
        if self.__lvl >= 100:
            self.__lvl = 100

    @classmethod
    def set_cls_field(cls):
        cls.__LVL = Player.__type_test(20)
        cls.__HEALTH = Player.__type_test(200)

    # Метод проверки типов (статический, а не property)
    @staticmethod
    def __type_test(value: int):
        if isinstance(value, int):
            return value
        raise TypeError("Это не число!")


user1 = Player()
print(user1.lvl)

Player.set_cls_field()
print()

user2 = Player()
print(user2.lvl)


Этот код — отличный пример продвинутого ООП в Python. В нём используются механизмы оптимизации памяти, защиты данных и разные типы методов.
Ниже подробно разобран каждый элемент, структура приватности и работа __slots__.
------------------------------
## 1. Как работает приватность и __slots__
В Python нет абсолютной приватности (как private в Java или C++), но есть механизм искажения имён (Name Mangling).
## Приватные переменные (__имя)
Когда вы создаете переменную с двумя подчеркиваниями (например, __lvl), Python автоматически переименовывает её внутри класса, добавляя префикс _ИмяКласса.

* __lvl превращается в _Player__lvl.
* __HEALTH превращается в _Player__HEALTH.

Это сделано для того, чтобы случайно не перезаписать эти переменные извне или в классах-наследниках.
## Зачем нужен __slots__ и почему там странные имена?
Обычно каждый объект в Python хранит свои локальные переменные в динамическом словаре __dict__. Это гибко (можно на лету добавить новое поле игроку: user1.mana = 50), но тратит очень много оперативной памяти.
__slots__ оптимизирует это:

* Он удаляет словарь __dict__ у объекта.
* Он жестко фиксирует список разрешенных полей в памяти. Создать поле mana на лету теперь нельзя.
* Связь с приватностью: Так как __slots__ проверяет точные имена переменных, а Python уже переименовал __lvl в _Player__lvl, вам обязательно нужно указывать в __slots__ именно искаженные имена: "_Player__lvl". Если написать просто "__lvl", Python не поймет связь и выдаст ошибку.

------------------------------
## 2. Подробный разбор всех методов## Конструктор: __init__(self)

* Что делает: Инициализирует (создает) каждый новый объект игрока.
* Зачем нужен: Он берет стартовые настройки из класса (Player.__LVL) и записывает их в память конкретного игрока (self.__lvl). Также он фиксирует точное время создания персонажа через datetime.now().

## Геттер: @property def lvl(self)

* Что делает: Позволяет читать приватные данные как обычное поле, а не как функцию (пишем user1.lvl, а не user1.lvl()).
* Зачем нужен: Он защищает переменную self.__lvl. Вместо того чтобы отдавать её напрямую, он "на лету" считает, сколько времени прошло с момента создания игрока (time_diff), и возвращает красивый кортеж: (уровень, время_в_игре).

## Сеттер: @lvl.setter def lvl(self, numeric: int)

* Что делает: Срабатывает, когда вы пытаетесь изменить уровень через знак равенства: user1.lvl = 5.
* Зачем нужен: Это главный инструмент инкапсуляции (защиты данных). Внутри него зашита логика:
1. Он отправляет число на проверку в __type_test.
   2. Он не просто заменяет уровень, а прибавляет его (+=).
   3. Он контролирует лимит: если уровень стал больше 100, он принудительно сбрасывает его до 100. Игрок не сможет сделать себе 999 уровень.

## Метод класса: @classmethod def set_cls_field(cls)

* Что делает: Работает не с конкретным игроком, а со всем шаблоном (классом) Player. Вместо self (экземпляр) он принимает cls (сам класс).
* Зачем нужен: Чтобы менять глобальные настройки для будущих объектов. Когда вы вызываете Player.set_cls_field(), вы меняете стандартные параметры __LVL и __HEALTH внутри самого шаблона. Именно поэтому user1 остался с 1 уровнем, а созданный после этого вызова user2 сразу получил стартовый 20 уровень.

## Статический метод: @staticmethod def __type_test(value: int)

* Что делает: Это обычная изолированная функция, которая просто "живет" внутри класса. Ей не нужен ни конкретный объект (self), ни сам класс (cls). Она видит только то, что ей передали в аргументы (value).
* Зачем нужен: Это утилита валидации. Она проверяет, является ли переданное значение целым числом (int). Если это строка или дробь, метод грубо прерывает программу ошибкой TypeError. Из-за двойного подчеркивания __type_test этот метод тоже скрыт от внешнего мира — его можно вызвать только изнутри самого класса.

------------------------------
Если вы хотите закрепить материал, мы можем добавить в set_cls_field аргументы, чтобы параметры не были жестко прописаны (20 и 200), а задавались вами при вызове. Хотите посмотреть, как передавать данные в методы класса?

